// 复制本文件为 secrets.dart 并填入你自己的密钥后即可构建。
// secrets.dart 已加入 .gitignore，不会被提交。

class Secrets {
  /// DeepSeek API Key 默认值。
  /// 留空字符串则要求用户必须在设置页填写。
  static const String defaultDeepseekApiKey = '';
}
